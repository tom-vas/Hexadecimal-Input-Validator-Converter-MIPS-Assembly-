# Hexadecimal-Input-Validator-Converter-MIPS-Assembly-
MIPS assembly program that reads a 4-byte hexadecimal input, validates each character, converts the result into its decimal value, and prints either the computed number or an error message.
